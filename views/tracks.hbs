<div class="track">
  {{#each tracks}}

    {{this.name}}

    {{#if this.preview_url}}
      <audio controls>
          <source src="{{this.preview_url}}" type="audio/mpeg">
          Your browser does not support the audio tag.
      </audio>
    {{else}}
        <p><b>No preview available! Sorry!</b></p>
    {{/if}}

  {{/each}}
</div>